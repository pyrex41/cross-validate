apiVersion: apps/v1
kind: Deployment
metadata:
  name: mismatch-app
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: mismatch
  template:
    metadata:
      labels:
        app: mismatch
    spec:
      containers:
        - name: app
          image: nginx:1.27
