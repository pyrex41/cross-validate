apiVersion: apps/v1
kind: Deployment
metadata:
  name: ref-app
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: ref-app
  template:
    metadata:
      labels:
        app: ref-app
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
