apiVersion: apps/v1
kind: Deployment
metadata:
  name: ok-app
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: ok-app
  template:
    metadata:
      labels:
        app: ok-app
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
